#include <stdio.h>
#include <math.h>

int main()
{

    /*

     int b, c, d;
     b = c = d = 3;
     int power;
     power = pow(b, c);
     printf("Power Result : %d", power);

    */

    /*
    Modular Operator (%)
    */

    // int a = 5, b = 3;
    // int modular = a % b;
    // printf("%d", modular);
    // float a = 1.99999;

    // printf("%f",a);

    /*


    Associativity (for same precendence)
    Left to Right

    calculate x value of x= 4*3/6*2;


    */

        printf("%d", (4==3) && (3>=3));
    return 0;
}